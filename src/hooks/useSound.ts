// Re-exportujeme vše ze SoundContextu
export { useSound } from '../context/SoundContext';
// A pokud někdo potřebuje typ (i když ten je teď v types/index.ts)
export type { SoundType } from '../types';